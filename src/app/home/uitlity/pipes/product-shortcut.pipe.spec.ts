import { ProductShortcutPipe } from './product-shortcut.pipe';

describe('ProductShortcutPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductShortcutPipe();
    expect(pipe).toBeTruthy();
  });
});
